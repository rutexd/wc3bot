[package]
name = "takecell"
version = "0.1.2"
edition = "2018"
description = "A cell type which value can only be taken once (vendored fork without the rustc >= 1.96 requirement)"
license = "MIT"
repository = "https://github.com/wafflelapkin/takecell"

[dependencies]
